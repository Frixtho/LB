{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p database && touch database/database.sqlite && php artisan migrate --force && php artisan db:seed --force && node_modules/.bin/vite build --manifest-assets-files || true && php artisan serve --host 0.0.0.0 --port $PORT",
    "restartPolicy": "ON_FAILURE"
  }
}